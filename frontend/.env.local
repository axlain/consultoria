# Entorno DEMO — modo mock, sin credenciales reales
# Este archivo está comprometido intencionalmente en la rama demo.

# Sin VITE_SUPABASE_URL → supabase.js devuelve null → auth usa JWT propio
# Sin VITE_STRIPE_PK → StepCheckout usa formulario mock en lugar de Stripe Elements
