# Entorno DEMO — modo mock, sin credenciales reales
# Este archivo está comprometido intencionalmente en la rama demo.
# No contiene ninguna credencial de producción.

JWT_SECRET=demo-mode-axlain-not-for-production
PAYMENT_PROVIDER=mock
# SUPABASE_URL y SUPABASE_SERVICE_KEY NO están definidos → usa store en memoria
# STRIPE_SECRET_KEY NO está definido → usa mock provider
