{
  "name": "@consultoria/core-booking-engine",
  "private": true,
  "version": "1.0.0",
  "description": "Módulo compartido de pagos, mensajería y QR para sitios de negocio",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test:e2e": "playwright test",
    "lint": "tsc --noEmit --project tsconfig.base.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.3.0"
  }
}
